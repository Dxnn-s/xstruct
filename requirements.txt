# Week 1 is stdlib-only — nothing required to run `python -m xstruct.cli`.
# Dev:
pytest>=8.0

# --- lands Week 2+ when real adapters arrive (kept here as the forward plan) ---
# httpx>=0.27          # async REST
# websockets>=12.0     # live book/trade streams
# pynacl>=1.5          # Ed25519 signing (Pascal)
# cryptography>=42.0   # RSA signing (Kalshi)
# hyperliquid-python-sdk
# pandas>=2.2
# polars>=1.0
# matplotlib>=3.8
