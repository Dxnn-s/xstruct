# Runtime: the paper adapter is stdlib-only; the Hyperliquid adapter needs httpx.
httpx>=0.27

# Dev
pytest>=8.0

# --- lands with signing + more adapters (Week 2b+) ---
# websockets>=12.0     # live book/trade streams
# pynacl>=1.5          # Ed25519 signing (Pascal)
# cryptography>=42.0   # RSA signing (Kalshi)
# hyperliquid-python-sdk
# pandas>=2.2
# matplotlib>=3.8
